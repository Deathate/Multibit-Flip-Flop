[package]
name = "num_cast"
version = "0.1.0"
edition = "2021"

[dependencies]
duplicate = "2.0.0"
easy-cast = { version = "0.5.3", features = ["always_assert"] }
# cast = "0.3.0"
# conv = "0.3.3"
cfg-if = "1.0.0"
[features]
always_assert = []
integer_as_i64 = []
float_as_f64 = []
default = ["always_assert", "float_as_f64"]
